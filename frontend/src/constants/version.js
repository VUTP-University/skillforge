/**
 * Single source of truth for the version line shown under the brand
 * wordmark (navbar + login/register). Bump both values together on
 * each release.
 */
export const APP_VERSION = "2.2.4";
export const RELEASE_DATE_LABEL = "Sep 26, 2026";
